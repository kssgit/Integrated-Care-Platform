{{- if .Values.apiEventDlqRetryWorker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "integrated-care.fullname" . }}-api-event-dlq-retry
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "integrated-care.labels" . | nindent 4 }}
    app: api-event-dlq-retry
spec:
  replicas: {{ .Values.apiEventDlqRetryWorker.replicaCount }}
  selector:
    matchLabels:
      app: api-event-dlq-retry
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: api-event-dlq-retry
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: api-event-dlq-retry
          image: "{{ .Values.apiEventDlqRetryWorker.image.repository }}:{{ .Values.apiEventDlqRetryWorker.image.tag }}"
          imagePullPolicy: {{ .Values.apiEventDlqRetryWorker.image.pullPolicy }}
          command: ["python", "-m", "api.event_dlq_retry"]
          envFrom:
            - configMapRef:
                name: {{ include "integrated-care.fullname" . }}-config
            {{- if .Values.secret.create }}
            - secretRef:
                name: {{ include "integrated-care.fullname" . }}-secret
            {{- end }}
          resources:
            {{- toYaml .Values.apiEventDlqRetryWorker.resources | nindent 12 }}
{{- end }}

